[build-system]
requires = ["hatchling>=1.24.2"]
build-backend = "hatchling.build"

[project]
name = "ontrackchain-shared"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.7.4",
]

[tool.hatch.build.targets.wheel]
packages = ["src/ontrackchain_shared"]
