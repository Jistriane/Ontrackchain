[build-system]
requires = ["hatchling>=1.24.2"]
build-backend = "hatchling.build"

[project]
name = "ontrackchain-platform"
version = "3.1.0"
description = "Ontrackchain Platform Workspace Root (Sprint 18)"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Proprietary" }
authors = [
  { name = "Ontrackchain" },
]
dependencies = [
  "ontrackchain-shared @ file:///${PROJECT_ROOT}/packages/shared",
  "ontrackchain-qa-gateway @ file:///${PROJECT_ROOT}/packages/qa-gateway",
  "ontrackchain-agents @ file:///${PROJECT_ROOT}/packages/agents",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0.0",
  "pytest-asyncio>=0.23.8",
  "httpx>=0.27.0",
  "ruff>=0.5.0",
  "mypy>=1.10.0",
  "types-requests",
  "types-PyYAML",
]

[tool.hatch.envs.default]
dependencies = [
  "ontrackchain-shared[dev] @ file:///${PROJECT_ROOT}/packages/shared",
  "ontrackchain-qa-gateway[dev] @ file:///${PROJECT_ROOT}/packages/qa-gateway",
  "ontrackchain-agents @ file:///${PROJECT_ROOT}/packages/agents",
  "pytest>=8.0.0",
  "pytest-asyncio>=0.23.8",
  "httpx>=0.27.0",
]
[tool.hatch.envs.default.extra-dirs]
"/app/src" = [
  "apps/auth-service/src",
  "apps/compliance-api/src",
  "apps/case-management/src",
  "apps/investigation-api/src",
  "apps/monitoring-api/src",
  "apps/public-api/src",
  "apps/report-api/src",
  "apps/ai-service/src",
  "apps/mock-oidc/src",
]

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = [
  "tests",
  "apps/auth-service/tests",
  "apps/compliance-api/tests",
  "apps/case-management/tests",
  "apps/investigation-api/tests",
  "apps/monitoring-api/tests",
  "apps/public-api/tests",
  "apps/report-api/tests",
  "apps/ai-service/tests",
  "apps/mock-oidc/tests",
  "packages/shared/tests",
  "packages/qa-gateway/tests",
  "packages/agents/tests",
]
asyncio_mode = "auto"
pythonpath = [
  ".",
  "packages/shared/src",
  "packages/qa-gateway/src",
  "packages/agents/src",
  "apps/auth-service/src",
  "apps/compliance-api/src",
  "apps/case-management/src",
  "apps/investigation-api/src",
  "apps/monitoring-api/src",
  "apps/public-api/src",
  "apps/report-api/src",
  "apps/ai-service/src",
  "apps/mock-oidc/src",
  "apps/auth-service/tests",
  "apps/investigation-api/tests",
  "apps/case-management/tests",
]

[tool.ruff]
line-length = 120
target-version = "py311"
lint.select = ["E", "F", "I", "N", "B", "UP", "W"]
lint.ignore = ["E501", "B008"]
lint.isort.known-first-party = [
  "ontrackchain_shared",
  "qa_gateway",
  "ontrackchain_agents",
  "auth_service",
  "compliance_api",
  "case_management",
  "investigation_api",
  "monitoring_api",
  "public_api",
  "report_api",
  "ai_service",
  "mock_oidc",
]

# ============================================================
# Sprint S28+28: [tool.mypy] SSOT workspace (8 apps + 3 packages)
# S28+31→S28+45: Roadmap incremental strict 1 app/sprint (10 milestones 10/10 ✅)
# S28+46 P4: ATIVAÇÃO STRICT GLOBAL DEFAULT — fecha o ciclo do incrementalismo.
#   · Qualquer NOVO módulo criado a partir de hoje JÁ NASCE em strict automaticamente (fail-fast).
#   · Os 12 overrides [[tool.mypy.overrides]] individuais PERMANECEM (redundância segura = defesa em profundidade).
#   · 9 flags idênticas em todos os níveis (default + overrides) garantem comportamento determinístico.
# ============================================================
[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_ignores = true
strict_equality = true
warn_unused_configs = true
disallow_untyped_defs = true
check_untyped_defs = true
disallow_incomplete_defs = true
disallow_untyped_calls = true
disallow_untyped_decorators = true
warn_redundant_casts = true
ignore_missing_imports = true
no_implicit_optional = true
show_error_codes = true
show_error_context = true
pretty = true
mypy_path = [
    "packages/shared/src",
    "packages/qa-gateway/src",
    "packages/agents/src",
    "apps/ai-service/src",
    "apps/auth-service/src",
    "apps/case-management/src",
    "apps/compliance-api/src",
    "apps/investigation-api/src",
    "apps/monitoring-api/src",
    "apps/public-api/src",
    "apps/report-api/src",
    "apps/mock-oidc/src",
]
exclude = [
    "apps/frontend",
    "packages/.*/tests",
    "apps/.*/tests",
    "tests",
    "__pycache__",
]
incremental = true
cache_dir = ".mypy_cache"

# ============================================================
# Sprint S28+31 P3: Strict mypy Incremental — Shared First
# S28+31→S28+45: 10 milestones 1 app/sprint (overrides individuais)
# S28+46 P4: ATIVAÇÃO STRICT GLOBAL DEFAULT [tool.mypy] (9 flags estritas)
#   · default agora = 9 flags strict; overrides individuais permanecem (redundância)
#   · NOVOS módulos sem override específico JÁ NASÇEM strict (fail-fast positivo)
# S28+32 P3: qa_gateway.* strict
# S28+33 P3: ontrackchain_agents.* strict (2629 LOC, 19 arquivos: sanctions_engine + evidence_trail + etc)
# S28+37 P3: auth_service.* strict (PRIMEIRO APP: 2737 LOC, main.py 2473L FastAPI auth)
# Roadmap ADR mypy incremental strict Shared → QA → Agents → 9 Apps 1×1 → Global Default:
#   · S28+31 ✅ packages/shared/src       (SSOT fonte da verdade)
#   · S28+32 ✅ packages/qa-gateway/src   (9 comandos CLI + policies + rls guard)
#   · S28+33 ✅ packages/agents/src       (2629 LOC: sanctions/evidence/preventive_block/travel_rule)
#   · S28+37 ✅ apps/auth-service         (PRIMEIRO APP: OIDC, 2FA, B2B HMAC, secrets_loader)
#   · S28+38 ✅ apps/compliance-api       (segundo APP: 6420 LOC, structural_screens + risk_provider 28 arquivos)
#   · S28+39 ✅ apps/case-management      (terceiro APP: 1353 LOC FastAPI CRUD casos)
#   · S28+40 ✅ apps/investigation-api    (quarto APP: 8485 LOC main 5968L + billing/stripe/worker/ai/graph intel 12 arquivos)
#   · S28+41 ✅ apps/monitoring-api     (quinto APP: 2699 LOC main.py FastAPI SLO + healthz endpoints)
#   · S28+42 ✅ apps/public-api         (sexto APP: 886 LOC main.py FastAPI endpoints público sem RBAC)
#   · S28+43 ✅ apps/report-api         (sétimo APP: 2737 LOC main.py FastAPI relatórios PDF/dashboards)
#   · S28+44 ✅ apps/ai-service         (oitavo APP: 3710 LOC main 2590L + agent_framework LLM 854L + worker 265L, ÚLTIMO APP REAL)
#   · S28+45 ✅ apps/mock-oidc         (nono APP ÚLTIMO: 696 LOC main.py mock OIDC, 🏁 ROADMAP 10 MÓDULOS 10/10 ✅! 12 MÓDULOS STRICT: 3 shared + 9 apps)
#   · S28+46 ✅ DEFAULT [tool.mypy]    (🏁 FECHAMENTO CICLO STRICT GLOBAL! 9 flags estritas no bloco default. Novos módulos nascem strict automaticamente. 12 overrides permanecem = defesa em profundidade)
#   · S28+47 ✅ SonarCloud Quality     (Sincronia paths sonar-project.properties RAIZ ↔ ci.yml args + ROOTS coverage 7→13 serviços + CodeCov wildcard + Bandit SARIF → GH Code Scanning + arquivo subpasta marcado OBSOLETO)
#   · S28+48 ✅ Logging Estruturado    (shared ontrackchain_shared/logging_util.py stdlib + json.dumps + contextvars, 0 deps novas. Habilitado em auth-service, public-api, case-management. X-Request-Id middleware + service field padrão + severity field)
#   · S28+49 ✅ Makefile Extras        (make format = ruff format apps/packages/scripts; make audit = pip-audit 13 serviços resumido NB; make clean = tmp_* + __pycache__ + .pytest_cache + .mypy_cache)
#   · S28+50 ✅ Docker Otimizado       (11 Dockerfiles Python: non-root UID/GID 10001 appuser + PYTHONDONTWRITEBYTECODE=PYTHONUNBUFFERED=1 + HEALTHCHECK urllib /healthz zero deps + camadas COPY ordenadas (shared → agents → pyproject → src) + remoção extras [dev] runtime em auth/case/investigation/ai + rm /tmp/ontrackchain-* após pip + PID 1 python via CMD sh exec + docker-compose.yml corrige 4/4 build.context (context: . + dockerfile: apps/X) em auth/public/case/mock)
#   · S28+51 ✅ Makefile Compose Conv  (5 novos targets NÃO-gating: scan-secrets-strict (alias trufflehog curto), e2e-light (wrapper s28p27-run-e2e-light.sh perfil LEVE), compose-up-full (21 services stack completa observabilidade+9apps+3workers), compose-health (ps tabela formatada health), compose-purge (down -v destrutivo COM FORCE_PURGE=1 obrigatório, fail-safe default) + 5 linhas help + .PHONY atualizado)
#   · S28+52 ✅ Compose DependsOn HC   (13 relações service_started → service_healthy: CAT A 8 relações dict (redis HC compose + 4 apps HC Dockerfile investigation/compliance/monitoring/ai + case depende de ai) + CAT B 5 relações estilo LIST → DICT (prometheus depende de 4 apps + alertmanager, grafana depende prometheus). Resultado: 15 service_healthy / 10 service_completed_successfully / 2 service_started (alertmanager/grafana 3rd-party sem HC). Eliminado race conditions e2e-light.)
#   · S28+53 ✅ Logging Estruturado 10Apps (6 apps FastAPI restantes (compliance/investigation/monitoring/report/ai/mock-oidc) habilitados com bloco padrão S28+48. Total 3+6 = 9 apps + mock-oidc = 10/10 serviços FastAPI com JSON Lines + RequestIdLogMiddleware. AST parse 6/6 PASS, 0 regressão sintaxe.)
#   · S28+54 ✅ Makefile CI Conveniência (4 targets novos NÃO-gating encapsulam 8 gates padrão FAIL-CLOSED em 1 comando: ci-validate (4 gates <10s), ci-local (8 gates ~40s, commit), ci-pre-merge (8+lint+test-shared ~120s, PR/push), ci-smoke (qa-gateway-smoke rápido). .PHONY 4 novos + bloco help 6 linhas inserido + 4 targets body fim arquivo com TAB ASCII 0x09 validado. 4/4 make -n parse PASS.)
#   · S28+55 ✅ Governança Hardening CONTRIBUTING + .gitignore (CONTRIBUTING.md NOVO 6 seções: HC-1..HC-4 hard constraints, ciclo sprint 5 passos, 8 gates tabela, estrutura monorepo, padrão commit, checklist pré-commit. .gitignore organizado 8 blocos com hardening segredos .env*.bak/.local + tmp_qa/tmp_audit subdir + venv + compose.override.yml + caches hatch/coverage. 2 arquivos + 2 docs atualizados.)
#   · S28+56 ✅ Changelog Histórico Sprints + target make changelog (CHANGELOG-SPRINTS.md NOVO: tabela resumo 24 sprints S28+29..S28+55 + detalhe por sprint ordem recente primeiro + legenda prioridades. Target make changelog NÃO-gating: header bonito + cat arquivo, TAB ASCII 0x09 15/15 linhas validadas.)
#   · S28+57 ✅ Target contributing conveniência (target NÃO-gating simétrico S28+56 changelog: header bonito 7 linhas echo + cat CONTRIBUTING.md (TAB ASCII 0x09 10/10 linhas validadas. Header CI Conveniência Makefile adicionado 1 linha contributing help + .PHONY contributing atualizado.)
#   · S28+58 ✅ .editorconfig Hardening Governança (SSOT convenções IDE monorepo 6 seções: root=true + defaults UTF-8 LF + [*.py] indent4 + [*.{toml,yml,yaml,md}] indent2 + [Makefile] indent_style=tab HARD REQUIREMENT (evita VSCode/JetBrains converter TAB ASCII 0x09 → spaces gerando missing separator GNU Make S28+49 legado) + [*.sh] indent2 + [*.json] indent2. insert_final_newline=true + trim_trailing_whitespace=true em todos. 0 alteração código runtime, 0 dependências novas.)
#   · S28+59 ✅ .gitattributes Hardening LF EOL Force Cross-OS (SSOT line endings + binários 3 blocos: BLOCO1 * text=auto eol=lf GERAL força LF em todos texto; BLOCO2 CRÍTICOS sobrescrita explícita eol=lf em *.sh/*.bash/Makefile/*.py/*.toml/*.yml/*.yaml/*.md/*.json (previne bash -n G3 QUEBRAR com \r + missing separator make S28+49 com \r no fim receitas); BLOCO3 BINÁRIOS 20 entradas explícitas marcadas binary (desativa diff/EOL/merge: 6 imagens + pdf + 2 bytecode python + 6 compactados + 5 fontes). Lote temático com S28+58 dotfiles governança cross-IDE/cross-OS 0 alteração código runtime.)
#   · S28+60 ✅ Target readme conveniência (TRINDADE docs 3/3: changelog S28+56 + contributing S28+57 + readme S28+60 100% SIMÉTRICOS). Makefile 3 edições Python literal \t TAB ASCII 0x09 HARD REQ: L1 .PHONY adicionado readme; L37 help CI header atualizado S28+57+60; L44 help nova linha make readme TAB 0x09; L685+ target readme 12 linhas simétrico (comentário Sprint + alvo ## help + 9 linhas body TAB 0x09 header 8 linhas echo bonito + @cat README.md). Validação pós-escrita Python 11/11 linhas TAB 0 ERROS. make -n help + make -n readme exit 0 parse OK. NÃO gating.)
#   · S28+61 ✅ CONTRIBUTING.md Hardening Checklist (HC-5 NOVO Dotfiles + Trindade Docs + 3 itens checklist novos). Tabela HC Seção 1 +HC-5 referenciando .editorconfig (S28+58) plugin IDE ativo + .gitattributes (S28+59) Windows autocrlf=input + 3 targets make changelog/contributing/readme (S28+60) validados via G3 bash + G5 all-checks parse Makefile. Seção 6 Checklist Rápido +3 itens NOVOS (7/8/9): 7) plugin EditorConfig ativo; 8) Windows autocrlf=input; 9) (opcional) 3 comandos make docs Trindade 3/3 executados. 0 alteração código negócio, 0 regressão.)
#   · S28+62 ✅ CHANGELOG-SPRINTS.md Atualização tabela + detalhe sprints S28+56→S28+61 (6 sprints recentes). Tabela Resumo Geral: header 24→30 sprints + 6 novas linhas topo tabela ordem recente-primeiro (S28+61 HC-5 → S28+60 readme → S28+59 gitattributes → S28+58 editorconfig → S28+57 contributing → S28+56 changelog init). Detalhe por Sprint: 6 blocos NOVOS topo (antes S28+55) com Objetivo + Entregas bullets + Arquivos + Impacto/Validação por sprint. 0 alteração código negócio. 0 regressão gates.)
#   · S28+63 ✅ README.md Sync Utilitários Dev + Governança 3 seções atualizadas. Header Utilitários Dev sprints 10→13 (+S28+60/+S28+61/+S28+62). 3 bullets desatualizados corrigidos: (a) CONTRIBUTING.md link 4→5 HC / 6→9 checklist; (b) make changelog 24→30 sprints S28+55→S28+61; (c) make contributing 4→5 HC / 6→9 checklist + info 3 itens novos. 2 bullets EXPLÍCITOS NOVOS no final Utilitários Dev: (1) S28+61 P4 Hardening HC-5 + 3 checklist (link CONTRIBUTING + validação G3+G5); (2) S28+62 P4 CHANGELOG-SPRINTS 30 sprints atualizado (6 sprints recentes). Fecha lote temático Governança DevX 13/13 sprints (S28+51→S28+63).)
#   · S28+64 ✅ CHANGELOG-SPRINTS.md Auto-Referencial Gap S28+62+S28+63. Sprint S28+62 atualizou tabela com S28+56→S28+61 mas os PRÓPRIOS S28+62 e S28+63 não estavam DENTRO do CHANGELOG (lacuna auto-referencial). Tabela Resumo Geral header 30→32 sprints + 2 novas linhas TOPO tabela (S28+63 README Sync + S28+62 CHANGELOG Atualização). Detalhe por Sprint: 2 BLOCOS NOVOS no TOPO (antes S28+61): S28+63 (3 bullets corrigidos + SSOT 4 docs) + S28+62 (6 sprints recentes tabela+detalhe). Fecha ciclo auto-referencial 100% sprints.)
#   · S28+65 ✅ README Troubleshooting Rápido 7 erros históricos + soluções 30 segundos. Nova seção README L156 ## Troubleshooting Rápido (antes ### 4. Arquivo .env local): 7 bullets padrão ❌ Erro → ✅ Solução → 🔍 Causa Sprint X: 1) Makefile missing separator S28+49; 2) py_compile Permission denied S28+48 → ast.parse pure-memory; 3) sandbox all-checks hatch pyc perm denied S28+50 → -n dry-run; 4) G3 bash \\r CRLF S28+59 → Windows autocrlf=input; 5) S28+53 AST depth bug caractere → linha-inteira count; 6) S28+52 compose depends_on LIST→DICT; 7) Web3 anti-padrões tx.origin / loops ilimitados / require strings. 2 blocos código (Python AST depth + YAML Compose DICT). 0 regressão.)
#   · S28+66 ✅ CHANGELOG Auto-Referencial Gap 2 (34 sprints, quebra ciclo infinito 1-shot adicionando S28+64 + S28+65 + a si mesmo S28+66 de uma vez). IMPL 1/3 Tabela Resumo Geral header 32→34 sprints + 3 NOVAS linhas TOPO tabela (S28+66 Gap2 → S28+65 Troubleshooting → S28+64 Gap1). IMPL 2/3 Detalhe por Sprint: 3 BLOCOS NOVOS topo (antes S28+63): S28+66 auto-referencial quebrado 3-em-1 + S28+65 Troubleshooting 7 erros + S28+64 Gap1 S28+62/S28+63. IMPL 3/3 pyproject roadmap linha S28+66 + docs chain fim atualizado. 0 impacto gates, 0 regressão, 0 código negócio alterado (HC-4 OK). Fecha Lote Temático Governança DevX 16/16 sprints (S28+51→S28+66).)
#   · S28+67 ✅ Tier0 Onboarding Docs Sync COMPLETO (4 arquivos: README raiz + CONTRIBUTING + CHANGELOG LEGADO + pyproject roadmap). (1) README.md: (a) Header Utilitários Dev sprints 10→16 (+S28+63/+S28+64/+S28+65/+S28+66); (b) 4 bullets NOVOS final Utilitários Dev com resumo S28+63 (Sync README), S28+64 (Gap1 Change), S28+65 (Troubleshooting 7 erros), S28+66 (Gap2 ciclo quebrado 34 sprints); (c) NOVA seção ## Mapa Completo da Documentação 7 Tiers Hierárquicos (T0 Onboarding → T1 Arquitetura+ADRs → T2 Governança → T3 Operações → T4 Evidência → T5 GitHub → T6 READMEs App → T7 Histórico Imutável NÃO ATUALIZAR) + tabela SSOT navegação + 2 dicas Trindade Docs + Troubleshooting. (2) CONTRIBUTING.md header ciclo sprint: 5 passos + COMMIT → 6 passos canônicos. (3) CHANGELOG.md LEGADO: banner topo DEPRECATED + link SSOT CHANGELOG-SPRINTS.md 34 sprints atualizado + aviso referência histórica pré-2026-08-10. 0 alteração código negócio, 0 regressão gates. Início batch 10 sprints Atualização Documentação Completa (S28+67→S28+76).)
#   · S28+68 ✅ Tier5 GitHub Triplicados SSOT Documentado (3 NOVOS READMEs internos + pyproject roadmap). (1) .github/README.md NOVO: SSOT ATIVO CI/CD GOVERNANÇA — 20 workflows reais executam aqui (GitHub só lê .github/workflows na RAIZ!); settings.yml HC-3 protegido G8; aviso workflows em subpastas NÃO EXECUTAM. (2) ontrackchain/.github/README.md NOVO: MIRROR COLABORAÇÃO compartilhado NÃO-EXECUTÁVEL — ISSUE_TEMPLATE 3 templates + CODEOWNERS + dependabot.yml ficam aqui como SSOT colaboração e são copiados para raiz periodicamente; workflows 10 arquivos aqui NÃO EXECUTAM (legado referência); settings.yml aqui DESCONTINUADO (usar raiz). (3) github_main/.github/README.md NOVO: LEGADO ARQUIVADO REFERÊNCIA HISTÓRICA — PROIBIDO USAR; workflows 18 arquivos (FALTAM 2 vs SSOT); settings.yml ausente; templates/CODEOWNERS/dependabot ausentes. Regra de navegação: README.md raiz ## Mapa Completo Documentação Tier5 lista status dos 3 diretórios. 0 alteração settings.yml/workflows (HC-3 intacto), 0 regressão gates.)
#   · S28+69 ✅ Tier1 Core Arquitetura Sync (3 arquivos docs: architecture.md + TECHNICAL_APPENDIX.md + EXECUTION_CHECKLIST_TO_95_PERCENT.md). (1) architecture.md: Decisões Arquiteturais Atuais 3→9 novas decisões (4) mypy strict S28+46, (5) QA Gateway ADR-029 S28+47→50, (6) TruffleHog Push Protection S28+36 ADR-023, (7) 15 Gates all-checks FAIL-CLOSED S28+54, (8) HC-5 Dotfiles + Trindade Docs S28+58→60, (9) CHANGELOG Hierárquico Sprint Format S28+56 ADR-023 pt2. Cada decisão tem Decisão/Justificativa/Gate/Proibido. NÃO ALTERADO ADRs 001-029. (2) TECHNICAL_APPENDIX.md: NOVA seção ## Governança e Qualidade (S28+30→S28+68) — Tabela 8 Gates G1→G8 padrão OBRIGATÓRIO (nome, como rodar, o que valida), Stack Atualizado 5 itens, Bloco código como rodar o ciclo completo VAL, Convenções Linguagem Dual pt-BR docs vs EN código. (3) EXECUTION_CHECKLIST_TO_95_PERCENT.md: NOVA seção ## Checklist Padrão de Execução Sprint 6 passos canônicos INV/DESIGN/IMPL/DOCS/VAL/COMMIT (cada passo com checkboxes concretos + 8 Gates checklist) + NOVA seção ## Checklist HC-5 Dotfiles com 4 passos máquina nova plugin EditorConfig LF Trindade Docs pull.rebase. 0 alteração ADRs, 0 regressão, 0 código negócio (HC-4 OK).)
#   · S28+70 ✅ Tier3 Operações Core Sync (3 arquivos docs: ci-cd-and-release.md + environment-variables.md + rbac-and-permissions.md). (1) ci-cd-and-release.md: NOVA seção ## HC-3 Branch Protection Required Contexts — Tabela 21 contexts main / 13 develop / QA Gate 2 jobs obrigatórios + 0 sonarcloud-* (HC-3 bloqueia); 3 Environments protegidos; 14 Labels obrigatórios; comando validador make settings-dry-run; Push Protection + GHAS habilitados. (2) environment-variables.md: NOVA seção ## Billing Stripe + Integrações Financeiras (6 vars: STRIPE_SECRET_KEY / PUBLISHABLE / WEBHOOK_SECRET / TAX_BEHAVIOR / BILLING_FAIL_CLOSED / RECEIPT_BUCKET + tabela placeholder HC-2 sem hardcoded) + NOVA seção ## Redis Fail-Closed (6 vars REDIS_HOST/PORT/DB/PASSWORD/FAIL_CLOSED=true/TIMEOUT_SEC + comportamento DOWN 503 vs continua + exemplo env-staging trecho Billing+Redis). (3) rbac-and-permissions.md: NOVA seção ## Bypass Documentado Healthz + Metrics G4 18/18 — tabela 9 serviços FastAPI ×2 endpoints =18 bypass; motivo K8s health probe + Prometheus scrape; middleware FastAPI padrão if path in {/healthz,/metrics}; 3 items PROIBIDO; comando validador bash s28p24-check-healthz-metrics-bypass.sh. 0 regressão gates HC, 0 código negócio alterado.)
# Docs: ADR mypy incremental strict Shared → QA → Agents → 9 Apps 1×1 → Global Default Strict → SonarCloud QG Sync → Structured Logging Stdlib → Makefile Extras → Docker Otimizado → Makefile Compose Conveniência → Compose DependsOn Healthy → Logging Estruturado 10Apps → Makefile CI Conveniência → Governança Hardening CONTRIBUTING + .gitignore → Changelog Histórico Sprints → Target contributing conveniência → .editorconfig Hardening Governança → .gitattributes Hardening LF EOL Force → Target readme conveniência → CONTRIBUTING.md Hardening Checklist HC-5 + 3 itens → CHANGELOG-SPRINTS.md Atualização 6 sprints recentes → README Sync Utilitários Dev + Governança → CHANGELOG-SPRINTS.md Auto-Referencial Gap S28+62+S28+63 32 sprints → README Troubleshooting Rápido 7 erros históricos → CHANGELOG Auto-Referencial Gap 2 (34 sprints, ciclo quebrado 1-shot S28+66) → Tier0 Onboarding Docs Sync 4 arquivos (Mapa 7 Tiers + CHANGELOG LEGADO DEPRECATED + CONTRIBUTING 6 passos + S28+67) → Tier5 GitHub Triplicados SSOT Documentado 3 READMEs internos (S28+68) → Tier1 Core Arquitetura Sync 3 arquivos docs 9 decisões + 8 Gates tabela + Checklist Execução Sprint (S28+69) → Tier3 Operações Core Sync 3 docs (HC-3 contexts + Billing Stripe + Redis Fail-Closed + RBAC Bypass 18/18) (S28+70).
# ============================================================
[[tool.mypy.overrides]]
module = "ontrackchain_shared.*"
disallow_untyped_defs = true
warn_return_any = true
warn_unused_ignores = true
strict_equality = true
check_untyped_defs = true
disallow_incomplete_defs = true
disallow_untyped_calls = true
disallow_untyped_decorators = true
warn_redundant_casts = true

[[tool.mypy.overrides]]
module = "qa_gateway.*"
disallow_untyped_defs = true
warn_return_any = true
warn_unused_ignores = true
strict_equality = true
check_untyped_defs = true
disallow_incomplete_defs = true
disallow_untyped_calls = true
disallow_untyped_decorators = true
warn_redundant_casts = true

[[tool.mypy.overrides]]
module = "ontrackchain_agents.*"
disallow_untyped_defs = true
warn_return_any = true
warn_unused_ignores = true
strict_equality = true
check_untyped_defs = true
disallow_incomplete_defs = true
disallow_untyped_calls = true
disallow_untyped_decorators = true
warn_redundant_casts = true

[[tool.mypy.overrides]]
module = "auth_service.*"
disallow_untyped_defs = true
warn_return_any = true
warn_unused_ignores = true
strict_equality = true
check_untyped_defs = true
disallow_incomplete_defs = true
disallow_untyped_calls = true
disallow_untyped_decorators = true
warn_redundant_casts = true

[[tool.mypy.overrides]]
module = "compliance_api.*"
disallow_untyped_defs = true
warn_return_any = true
warn_unused_ignores = true
strict_equality = true
check_untyped_defs = true
disallow_incomplete_defs = true
disallow_untyped_calls = true
disallow_untyped_decorators = true
warn_redundant_casts = true

[[tool.mypy.overrides]]
module = "case_management.*"
disallow_untyped_defs = true
warn_return_any = true
warn_unused_ignores = true
strict_equality = true
check_untyped_defs = true
disallow_incomplete_defs = true
disallow_untyped_calls = true
disallow_untyped_decorators = true
warn_redundant_casts = true

[[tool.mypy.overrides]]
module = "investigation_api.*"
disallow_untyped_defs = true
warn_return_any = true
warn_unused_ignores = true
strict_equality = true
check_untyped_defs = true
disallow_incomplete_defs = true
disallow_untyped_calls = true
disallow_untyped_decorators = true
warn_redundant_casts = true

[[tool.mypy.overrides]]
module = "monitoring_api.*"
disallow_untyped_defs = true
warn_return_any = true
warn_unused_ignores = true
strict_equality = true
check_untyped_defs = true
disallow_incomplete_defs = true
disallow_untyped_calls = true
disallow_untyped_decorators = true
warn_redundant_casts = true

[[tool.mypy.overrides]]
module = "public_api.*"
disallow_untyped_defs = true
warn_return_any = true
warn_unused_ignores = true
strict_equality = true
check_untyped_defs = true
disallow_incomplete_defs = true
disallow_untyped_calls = true
disallow_untyped_decorators = true
warn_redundant_casts = true

[[tool.mypy.overrides]]
module = "report_api.*"
disallow_untyped_defs = true
warn_return_any = true
warn_unused_ignores = true
strict_equality = true
check_untyped_defs = true
disallow_incomplete_defs = true
disallow_untyped_calls = true
disallow_untyped_decorators = true
warn_redundant_casts = true

[[tool.mypy.overrides]]
module = "ai_service.*"
disallow_untyped_defs = true
warn_return_any = true
warn_unused_ignores = true
strict_equality = true
check_untyped_defs = true
disallow_incomplete_defs = true
disallow_untyped_calls = true
disallow_untyped_decorators = true
warn_redundant_casts = true

[[tool.mypy.overrides]]
module = "mock_oidc.*"
disallow_untyped_defs = true
warn_return_any = true
warn_unused_ignores = true
strict_equality = true
check_untyped_defs = true
disallow_incomplete_defs = true
disallow_untyped_calls = true
disallow_untyped_decorators = true
warn_redundant_casts = true
