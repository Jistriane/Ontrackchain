version: "1"

previews:
  generation: off

services:
  - type: web
    runtime: docker
    name: ontrackchain-frontend-showcase-staging
    plan: starter
    dockerContext: ./apps/frontend
    dockerfilePath: ./apps/frontend/Dockerfile
    healthCheckPath: /api/healthz
    envVars:
      - key: APP_ENV
        value: staging
      - key: AUTH_MODE
        value: oidc
      - key: FRONTEND_STANDALONE_SHOWCASE_MODE
        value: "true"
      - key: NEXT_PUBLIC_APP_ENV
        value: staging
      - key: NEXT_PUBLIC_AUTH_MODE
        value: oidc
      - key: NEXT_PUBLIC_FRONTEND_STANDALONE_SHOWCASE_MODE
        value: "true"
      - key: NEXT_PUBLIC_FRONTEND_URL
        value: "https://ontrackchain-frontend-showcase-staging.onrender.com"
      - key: NEXT_PUBLIC_APP_URL
        value: "https://ontrackchain-frontend-showcase-staging.onrender.com"
